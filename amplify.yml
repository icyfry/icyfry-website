version: 0.1
frontend:
  phases:
    preBuild:
      commands:
        - npm install -g @vue/cli
        - npm install -g @vue/cli-service-global
    build:
      commands:
        - npm run build
  artifacts:
    baseDirectory: /dist
    files:
      - '**/*'
  cache:
    paths: []
